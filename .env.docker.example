# Docker Environment File Example
# Copy this file for each collection you want to compile
# Example: cp .env.docker.example collection1.env
#
# Usage: docker run --rm --env-file collection1.env -v $(pwd)/output:/app/output outline-compiler

# Required: Your Outline API key (get from Settings → API & Apps)
OUTLINE_API_KEY=your_api_key_here

# Required: Collection ID to compile
OUTLINE_COLLECTION_ID=your_collection_uuid_here

# Optional: Outline API URL (default: https://app.getoutline.com/api)
# Uncomment and modify if using self-hosted Outline
#OUTLINE_API_URL=https://your-outline-instance.com/api

# Optional: Output file path inside container (default: /app/output/compilation.html)
# Change this if running multiple collections to separate output files
#OUTLINE_OUTPUT_FILE=/app/output/my-collection.html
